# Deploy to IPFS
## Option 1: Fleek `npm i -g @fleekhq/cli && fleek sites:deploy`
## Option 2: Manual `cd out && curl -X POST -F file=@index.html https://ipfs.io/api/v0/add`
## Access: IPFS Gateway, ENS (economugin.eth), or Brave native IPFS
