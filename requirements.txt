# Synced with pyproject.toml [project.dependencies] — Phase 0 / F0.1
# Preferred install:
#   pip install -e ".[dev,worker]"
# Or:
#   pip install -r requirements.txt
# Lock (when network available):
#   pip freeze > requirements.lock.txt

fastapi>=0.115.0,<1.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12
websockets>=13.0
starlette>=0.40.0

sqlalchemy[asyncio]>=2.0.36
aiosqlite>=0.20.0
asyncpg>=0.30.0
alembic>=1.14.0
greenlet>=3.0.0

pydantic>=2.9.0
pydantic-settings>=2.6.0
email-validator>=2.2.0

python-jose[cryptography]>=3.3.0
cryptography>=43.0.0
passlib[bcrypt]>=1.7.4
argon2-cffi>=23.1.0

httpx>=0.27.0
python-dotenv>=1.0.1
orjson>=3.10.0

sentry-sdk[fastapi]>=2.18.0

# Dev (also in pyproject [dev])
pytest>=8.3.0
pytest-asyncio>=0.24.0
