import {useTranslation} from 'react-i18next';import {useState,useRef,useEffect} from 'react';
export default function LanguageSwitcher(){const{i18n}=useTranslation();const[open,setOpen]=useState(false);const ref=useRef<HTMLDivElement>(null);useEffect(()=>{const h=(e:MouseEvent)=>{if(ref.current&&!ref.current.contains(e.target as Node))setOpen(false)};document.addEventListener('mousedown',h);return()=>document.removeEventListener('mousedown',h)},[]);const L:{[k:string]:{flag:string,label:string}}={fa:{flag:'🇮🇷',label:'فارسی'},en:{flag:'🇬🇧',label:'English'}};const cur=L[i18n.language]||L.en;
return(<div ref={ref} className="relative"><button onClick={()=>setOpen(!open)} className="flex items-center gap-1.5 px-2.5 py-2 rounded-lg hover:bg-slate-800 text-slate-300 text-sm"><span className="text-base">{cur.flag}</span><span className="hidden sm:inline">{cur.label}</span></button>{open&&(<div className="absolute top-full mt-1 right-0 bg-slate-900 border border-slate-700 rounded-xl shadow-2xl z-50 min-w-[140px]">{['fa','en'].map(l=>(<button key={l} onClick={()=>{i18n.changeLanguage(l);setOpen(false)}} className={`flex items-center gap-2 w-full px-4 py-2.5 text-sm ${i18n.language===l?'bg-brand-600/20 text-brand-400':'text-slate-300 hover:bg-slate-800'}`}><span className="text-lg">{L[l].flag}</span><span>{L[l].label}</span></button>))}</div>)}</div>)}