import {useTranslation} from 'react-i18next';
interface Props {value:number;lower:number;upper:number;unit?:string}
export default function UncertaintyRange({value,lower,upper,unit}:Props){const{t}=useTranslation();return(<div className="space-y-1"><div className="flex items-baseline gap-2"><span className="text-lg font-bold text-white">{value.toFixed(2)}</span>{unit&&<span className="text-xs text-slate-400">{unit}</span>}</div><div className="flex items-center gap-2"><span className="text-xs text-slate-500 w-14">{lower.toFixed(2)}</span><div className="flex-1 h-1.5 bg-slate-700 rounded-full"><div className="h-full bg-brand-500/60 rounded-full" style={{width:`${Math.max(5,(value-lower)/(upper-lower||1)*100)}%`}}/></div><span className="text-xs text-slate-500 w-14 text-right">{upper.toFixed(2)}</span></div><span className="text-[10px] text-slate-500">{t('uncertainty.ci95')}</span></div>)}