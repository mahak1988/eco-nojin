# Leave empty to use Vite proxy (recommended for local):
# FE http://localhost:5173  →  proxy  →  BE http://127.0.0.1:8000
#
# Only set when frontend is hosted separately from the API:
# VITE_API_BASE_URL=http://localhost:8000

# VITE_API_BASE_URL=
VITE_DEFAULT_LANG=fa
