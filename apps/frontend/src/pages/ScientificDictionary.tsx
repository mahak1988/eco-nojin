import{useState}from'react';import{Search,BookOpen,ChevronDown}from'lucide-react';import{DICTIONARY}from'../data/scientific-dictionary';
const CATS=['All',...new Set(DICTIONARY.map(d=>d.category))];
export default function ScientificDictionary(){const[q,setQ]=useState('');const[cat,setCat]=useState('All');const[exp,setExp]=useState<string|null>(null);
const f=DICTIONARY.filter(d=>{const mq=!q||d.term.toLowerCase().includes(q.toLowerCase())||d.definition.toLowerCase().includes(q.toLowerCase());return cat==='All'||d.category===cat?mq:false});
return(<div className="p-6 space-y-6"><div><h1 className="text-2xl font-bold text-white flex items-center gap-2"><BookOpen className="w-6 h-6 text-brand-400"/>Scientific Dictionary</h1><p className="text-slate-400 text-sm mt-1">{DICTIONARY.length} concepts · {CATS.length-1} categories</p></div><div className="relative"><Search className="absolute left-3 top-1/2 -translate-y-1/2 w-4 h-4 text-slate-500"/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search concepts, formulas..." className="w-full bg-slate-900 border border-slate-700 rounded-lg pl-10 pr-4 py-2.5 text-sm text-white"/></div><div className="flex gap-2 flex-wrap">{CATS.map(c=>(<button key={c} onClick={()=>setCat(c)} className={`text-xs px-3 py-1.5 rounded-lg ${c===cat?'bg-brand-600 text-white':'bg-slate-800 text-slate-400'}`}>{c}</button>))}</div><div className="space-y-2">{f.map(d=>(<div key={d.term} className="card-hydroma cursor-pointer" onClick={()=>setExp(exp===d.term?null:d.term)}><div className="flex items-center justify-between"><div className="flex items-center gap-2"><ChevronDown className={`w-4 h-4 text-slate-500 transition-transform ${exp===d.term?'rotate-180':''}`}/><h4 className="font-medium text-white">{d.term}</h4><span className="text-[10px] bg-brand-600/20 text-brand-400 px-2 py-0.5 rounded-full">{d.category}</span></div><span className="text-[10px] text-slate-500">{d.source}</span></div>{exp===d.term&&<div className="mt-3 pt-3 border-t border-slate-700 space-y-2"><p className="text-sm text-slate-300">{d.definition}</p>{d.formula&&<div className="bg-slate-950 border border-slate-700 rounded-lg px-4 py-3 font-mono text-sm text-brand-300">{d.formula}</div>}<div className="flex gap-1.5 flex-wrap">{d.related.map(r=><span key={r} className="text-[10px] bg-slate-800 text-slate-400 px-2 py-0.5 rounded">{r}</span>)}</div></div>}</div>))}</div></div>)}