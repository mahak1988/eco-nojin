import{ResponsiveContainer,LineChart,Line,XAxis,YAxis,CartesianGrid,Tooltip,Legend}from'recharts';
interface Props{data:{t:number;value:number;upper?:number;lower?:number}[];lines:{key:string;color:string;label:string;dashed?:boolean}[];height?:number;title?:string}
export default function TimeSeriesChart({data,lines,height=300,title}:Props){return(<div className="card-hydroma">{title&&<h3 className="text-sm font-semibold text-slate-300 mb-2">{title}</h3>}<ResponsiveContainer width="100%" height={height}><LineChart data={data}><CartesianGrid strokeDasharray="3 3" stroke="#334155"/><XAxis dataKey="t" stroke="#64748b" tick={{fontSize:11}}/><YAxis stroke="#64748b" tick={{fontSize:11}}/><Tooltip contentStyle={{background:'#1e293b',border:'1px solid #334155',borderRadius:'8px'}}/><Legend/>{lines.map(l=>(<Line key={l.key} dataKey={l.key} stroke={l.color} strokeWidth={2} strokeDasharray={l.dashed?'5 5':undefined} dot={false} name={l.label}/>))}</LineChart></ResponsiveContainer></div>)}