import{ReactNode}from'react';
interface Props{icon:ReactNode;label:string;value:string|number;delta?:string;color?:string;size?:'sm'|'md'|'lg'}
export default function StatCard({icon,label,value,delta,color='brand',size='md'}:Props){const sz=size==='lg'?'text-2xl':size==='sm'?'text-base':'text-xl';return(<div className="card-hydroma"><div className="flex items-center gap-2 mb-2"><span className={`text-${color}-400`}>{icon}</span><span className="text-xs text-slate-400 truncate">{label}</span></div><div className={`${sz} font-bold text-white tabular-nums`}>{value}</div>{delta&&<span className={`stat-badge mt-1 inline-block ${delta.startsWith('+')||delta.startsWith('↑')?'bg-green-900/40 text-green-400':delta.startsWith('-')||delta.startsWith('↓')?'bg-red-900/40 text-red-400':delta==='OK'?'bg-green-900/40 text-green-400':delta==='WARN'?'bg-yellow-900/40 text-yellow-400':'bg-slate-800 text-slate-300'}`}>{delta}</span>}</div>)}