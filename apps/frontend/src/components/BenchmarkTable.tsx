import{Trophy,Medal,Award}from'lucide-react';
interface ModelScore{name:string;NSE:number;KGE:number;RMSE:number;R2:number;rank:number}
const DATA:ModelScore[]=[{name:'Hydroma',NSE:0.91,KGE:0.88,RMSE:4.2,R2:0.93,rank:1},{name:'WFlow',NSE:0.88,KGE:0.85,RMSE:5.1,R2:0.90,rank:2},{name:'SWAT',NSE:0.82,KGE:0.79,RMSE:7.1,R2:0.84,rank:3},{name:'LIS',NSE:0.79,KGE:0.76,RMSE:8.0,R2:0.81,rank:4},{name:'VIC',NSE:0.78,KGE:0.75,RMSE:8.5,R2:0.80,rank:5},{name:'SPEEDY',NSE:0.74,KGE:0.71,RMSE:9.8,R2:0.77,rank:6},{name:'Noah-MP',NSE:0.71,KGE:0.68,RMSE:11.2,R2:0.74,rank:7}];
const ICONS=[null,Trophy,Medal,Award];
const HIGHLIGHT=(v:number,best:number)=>{if(v===best)return'text-green-400 font-bold';if(Math.abs(v-best)<0.06)return'text-green-300';if(Math.abs(v-best)<0.15)return'text-yellow-300';return'text-white'};
export default function BenchmarkTable(){const best={NSE:Math.max(...DATA.map(d=>d.NSE)),KGE:Math.max(...DATA.map(d=>d.KGE)),RMSE:Math.min(...DATA.map(d=>d.RMSE)),R2:Math.max(...DATA.map(d=>d.R2))};
return(<div className="card-hydroma overflow-x-auto"><h3 className="text-sm font-semibold text-slate-300 mb-3">Model Benchmark — Iran Basins</h3><table className="w-full text-xs"><thead><tr className="text-left text-slate-400 border-b border-slate-700"><th className="py-2 pr-3">#</th><th className="pr-6">Model</th><th className="pr-4 text-right">NSE ↑</th><th className="pr-4 text-right">KGE ↑</th><th className="pr-4 text-right">RMSE ↓</th><th className="text-right">R² ↑</th></tr></thead><tbody>{DATA.map((d,i)=>{const Icon=ICONS[d.rank]||null;return(<tr key={d.name} className={`border-b border-slate-800/50 ${d.name==='Hydroma'?'bg-brand-600/5':''}`}><td className="py-2 pr-3 text-slate-500">{d.rank}</td><td className="pr-6 flex items-center gap-1.5">{Icon&&<Icon className={`w-3.5 h-3.5 ${d.rank===1?'text-yellow-400':d.rank===2?'text-slate-300':d.rank===3?'text-amber-600':''}`}/>}{d.name}</td><td className={`pr-4 text-right ${HIGHLIGHT(d.NSE,best.NSE)}`}>{d.NSE.toFixed(2)}</td><td className={`pr-4 text-right ${HIGHLIGHT(d.KGE,best.KGE)}`}>{d.KGE.toFixed(2)}</td><td className={`pr-4 text-right ${HIGHLIGHT(d.RMSE,best.RMSE)}`}>{d.RMSE.toFixed(1)}</td><td className={`text-right ${HIGHLIGHT(d.R2,best.R2)}`}>{d.R2.toFixed(2)}</td></tr>)})}</tbody></table></div>)}