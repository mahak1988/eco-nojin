version: '3.8'

services:
  backend:
    build: .
    ports:
      - "8000:8000"
    environment:
      - ENVIRONMENT=production
      - DATABASE_URL=sqlite+aiosqlite:///./econojin.db
    volumes:
      - ./apps:/app/apps
    restart: unless-stopped

  # frontend service can be added here using Node image
