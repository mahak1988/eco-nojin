# Optional Phase 3 scientific stack — install when running EO / GEE / spatial heavy jobs
# pip install -r requirements-scientific.txt

earthengine-api>=0.1.384
# STAC clients (Planetary Computer path)
pystac-client>=0.9.0
planetary-computer>=1.0.0
# Geo helpers
shapely>=2.1.2
# Keep asyncpg for Postgres phase3
asyncpg>=0.30.0
psycopg[binary]>=3.3.4
