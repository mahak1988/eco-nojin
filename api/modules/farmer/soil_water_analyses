# در api/modules/farmer/models.py (یا مسیر معادل)

from sqlalchemy.orm import relationship

class Farmer(Base):
    __tablename__ = "farmers"
    # ... فیلدهای موجود ...

    soil_water_analyses = relationship(
        "SoilWaterAnalysis",
        back_populates="farmer",
        lazy="selectin",
    )