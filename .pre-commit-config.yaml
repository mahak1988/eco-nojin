repos:
  - repo: local
    hooks:
      - id: staged-secret-scan
        name: Staged Secret Scan (fast, <1s)
        entry: python staged_scan.py
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-commit]
